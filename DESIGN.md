# 가계부 디자인 시스템

## 1. 제품과 디자인 방향

이 제품은 은행·카드 자동 연동 없이 사용자가 직접 수입, 지출, 저축, 투자를 기록하는
모바일 중심 가계부다. 디자인은 다음 인상을 목표로 한다.

- **따뜻하고 차분한 장부**: 순백색 대신 크림색 종이와 따뜻한 회색을 사용한다.
- **숫자가 먼저 보이는 정보 도구**: 장식보다 월 수지, 금액, 진행률, 추세의 위계를
  우선한다.
- **한 번에 한 열**: 320–480px 범위에서 단일 컬럼을 유지하고, 넓은 화면에서는 앱
  자체를 확장하지 않고 가운데 정렬한다.
- **평평한 계층**: 모든 내용을 카드로 감싸지 않는다. 중요한 표면만 카드로 올리고,
  같은 섹션의 반복 행은 여백과 구분선으로 나눈다.
- **의미가 분리된 색**: 자금 유형을 나타내는 데이터 색과 오류·주의 같은 UI 상태 색을
  서로 섞지 않는다.
- **계산과 생성의 경계**: 원장에서 계산된 값은 흰 종이 표면에, AI가 작성한 문장은
  회색 채움 표면에 놓아 신뢰 경계를 드러낸다.
- **작지만 조작 가능한 모바일 UI**: 시각적으로 작은 아이콘도 44px 수준의 터치 영역을
  확보하며, 하단 탭과 바텀 시트를 핵심 탐색 패턴으로 사용한다.

## 2. 정보 구조

### 2.1 화면 구조

| 경로            | 화면            | 주 역할               | 핵심 UI                                              |
| --------------- | --------------- | --------------------- | ---------------------------------------------------- |
| `/login`        | 로그인·회원가입 | 계정 진입             | 제품 설명, 2분할 선택, 이메일·비밀번호 폼            |
| `/dashboard`    | 요약            | 특정 월의 상태 파악   | 월 탐색, 수지 히어로, 자금 흐름, 달성 확인, AI, 차트 |
| `/transactions` | 내역            | 거래 검색·조회·수정   | 월 탐색, 검색, 필터, 날짜별 목록, 엑셀 내보내기      |
| `/reports`      | 통계            | 3·6·12개월 분석       | 기간 칩, 추세·수지·구성·카테고리 차트                |
| `/settings`     | 설정            | 원장과 입력 기준 관리 | 일반, 카테고리, 고정 항목, AI, 계정                  |

인증 후 화면은 고정 하단 탭을 공유한다. 탭 순서는 **요약 → 내역 → 추가 → 통계 → 설정**이다.
가운데 `＋`는 별도 화면으로 이동하지 않고 어느 탭에서나 거래 추가 바텀 시트를 연다.

### 2.2 주요 사용자 흐름

1. 로그인한다.
2. 요약에서 선택한 달의 수지와 예산·목표 상태를 먼저 확인한다.
3. 가운데 `＋`로 거래를 기록하거나 내역에서 기존 거래를 수정한다.
4. 통계에서 여러 달의 흐름을 비교한다.
5. 설정에서 카테고리, 예산·목표, 고정 항목과 AI 사용 여부를 관리한다.

## 3. 레이아웃 시스템

### 3.1 앱 셸

- 앱 최대 너비: **480px**
- 최소 지원 기준: **320px**
- 넓은 뷰포트: `canvas` 배경 위에 480px 앱 열을 가운데 정렬
- 앱 내부 배경: `screen`
- 화면 본문 좌우 패딩: **16px**
- 화면 본문 상단 패딩: **16px**
- 기본 화면 세로 간격:
  - 요약: **24px**
  - 내역: **16px**
  - 통계: **12px**
  - 설정의 큰 섹션: **28px**
- 문서 전체가 기본 스크롤 컨테이너이며, 화면 전환 시 스크롤을 최상단으로 되돌린다.

### 3.2 상단 앱 바

- 높이: **56px**
- 배경: `screen`
- 하단: `line-soft` 1px 구분선
- 좌우 패딩: **16px**
- 왼쪽·오른쪽 슬롯의 최소 너비: **40px**
- 제목: `section` 타이포그래피
- 기본 제목은 중앙 정렬하지만, 월 탐색이 주인 요약 화면은 시각 제목을 숨기고 월 탐색을
  왼쪽에 둔다.
- 상단 바 자체는 sticky나 fixed가 아니다. 현재 문서 스크롤 구조에서는 화면 내용과 함께
  위로 스크롤된다.

### 3.3 화면 본문과 하단 탭

- 본문은 하단 탭과 iOS 홈 인디케이터를 피하도록
  `6.5rem + safe-area-inset-bottom`만큼 아래 여백을 둔다.
- 하단 탭은 화면 아래에 고정되고 최대 너비 480px를 따른다.
- 배경: `paper`
- 상단: `line` 구분선
- 그림자: `nav`
- 기본 탭의 최소 높이: **56px**
- 가운데 추가 버튼: **48×48px**, 탭 바 위로 20px 올라온 원형 버튼
- 현재 탭은 다음 세 신호를 함께 사용한다.
  - 아이콘과 글자색이 진한 `ink`
  - 아이콘 뒤에 `fill2` 캡슐
  - 더 강한 라벨 굵기

### 3.4 바텀 시트

- 네이티브 `<dialog>` 기반 모달
- 너비: 화면 전체, 최대 **480px**
- 최대 높이: **85dvh**
- 위쪽 모서리: `hero` 반경
- 배경: `paper`
- 그림자: `sheet`
- 상단에 36×4px 드래그 핸들이 있으나, 실제 드래그 동작은 없다.
- 내부가 길면 시트 본문만 세로 스크롤한다.
- 하단은 `20px + safe-area-inset-bottom`을 확보한다.
- 닫기 방법:
  - 우측 상단 닫기 버튼
  - Escape
  - 브라우저 지원 시 바깥 영역 클릭
  - Safari용 바깥 클릭 보완 처리
- 열릴 때 거래 추가는 금액 필드로, 그 외 시트는 제목으로 초점을 이동한다.

## 4. 색상

색상 원본은 `src/index.css`의 Tailwind `@theme` 토큰이다.

### 4.1 표면

| 토큰     | 값        | 역할                                     |
| -------- | --------- | ---------------------------------------- |
| `canvas` | `#f1eee7` | 480px 앱 밖의 페이지와 로그인 전체 배경  |
| `screen` | `#f8f6f1` | 인증 후 앱 셸과 화면 본문 배경           |
| `paper`  | `#fffdf9` | 카드, 입력, 탭 바, 시트의 가장 밝은 표면 |

세 표면은 모두 따뜻한 중성색이다. `canvas → screen → paper` 순으로 밝아져 앱 영역과
콘텐츠 표면을 그림자 없이 구분한다.

### 4.2 텍스트와 중성색

| 토큰        | 값        | 사용                                            |
| ----------- | --------- | ----------------------------------------------- |
| `ink`       | `#27251f` | 제목, 주요 숫자, 기본 아이콘, 주요 버튼         |
| `ink2`      | `#6f6a60` | 설명, 보조 라벨, 부가 정보                      |
| `ink3`      | `#8a8479` | 플레이스홀더, 비활성 아이콘, 차트 눈금, 장식 점 |
| `line`      | `#ded8ce` | 카드·입력의 기본 경계                           |
| `line-soft` | `#eae5dc` | 행과 셀 사이의 낮은 강도 구분선                 |
| `fill1`     | `#f1eee7` | AI 블록, 호버, 보조 패널                        |
| `fill2`     | `#e6e1d8` | 진행률 트랙, 선택 탭 캡슐, 스켈레톤             |
| `fill3`     | `#d8d2c7` | 꺼진 토글, 시트 핸들, 중성 채움                 |

`ink3`는 본문 텍스트가 아니라 비텍스트 요소와 보조 표시 전용이다. 읽어야 하는 문장은
`ink` 또는 `ink2`를 사용한다.

### 4.3 자금 데이터 색

| 의미 | 토큰              | 값        | 대표 사용                     |
| ---- | ----------------- | --------- | ----------------------------- |
| 수입 | `fund-income`     | `#596d73` | 수입 금액, 흐름 점·막대, 차트 |
| 지출 | `fund-expense`    | `#9b6b5d` | 지출 시리즈, 음수 수지 영역   |
| 저축 | `fund-saving`     | `#6e7d66` | 저축 시리즈, 양수 수지 영역   |
| 투자 | `fund-investment` | `#6c7488` | 투자 시리즈                   |

이 색은 **데이터 분류에만** 사용한다. 오류나 성공 상태를 표현하지 않는다.

### 4.4 UI 상태 색

| 의미      | 토큰             | 값        | 대표 사용                                  |
| --------- | ---------------- | --------- | ------------------------------------------ |
| 오류·초과 | `status-danger`  | `#b25147` | 오류 배너, 필드 오류, 삭제 확인, 음수 수지 |
| 주의·근접 | `status-warning` | `#8a6a2f` | 권한 오류, AI 경고, 주의·근접 상태         |
| 정상·달성 | `status-success` | `#4f765b` | 양수 수지, 정상·달성 상태, 성공 메시지     |
| 정보·진행 | `status-info`    | `#536f91` | 정보 안내, 진행 중 상태, AI 초안 안내      |

상태는 색만으로 전달하지 않는다. 상태 이름, 설명, 아이콘, 배경 틴트를 함께 쓴다.

### 4.5 카테고리 차트 팔레트

카테고리별 시리즈는 다음 순서로 반복된다.

1. `#9b6b5d`
2. `#6c7488`
3. `#8d7f57`
4. `#6e7d66`
5. `#596d73`
6. `#a98f80`
7. `#8a8479`

상태 색과 겹쳐 읽히지 않도록 자금 색을 중심으로 구성한 저채도 팔레트다.

### 4.6 대비 의도

- `ink`와 `ink2`는 `paper` 위 일반 텍스트용 대비를 목표로 한다.
- `ink3`는 일반 텍스트 대비가 아니라 아이콘, 입력 플레이스홀더, 비활성 컨트롤, 차트
  눈금 같은 비텍스트 요소의 3:1 기준을 목표로 한다.
- `status-warning`은 12px caption에서도 AA 대비를 확보하기 위해 기존 후보색보다 어두운
  `#8a6a2f`를 사용한다.
- 상태 배경은 단독으로 의미를 전달하지 않는 저농도 틴트이며, 같은 계열의 글자·아이콘·상태
  이름과 함께 사용한다.

### 4.7 색상 모드

- 현재는 **라이트 모드만 지원**한다.
- `<meta name="color-scheme" content="light">`로 브라우저 기본 컨트롤도 라이트 모드로
  고정한다.
- 브라우저 테마 색은 `canvas`와 같은 `#f1eee7`이다.
- 다크 모드 토큰이나 전환 UI는 없다.

## 5. 타이포그래피

### 5.1 글꼴

- 기본: **IBM Plex Sans KR**
- 숫자·고정폭 보조: **IBM Plex Mono**
- 대체: `system-ui`, `sans-serif`, `ui-monospace`, `monospace`
- 웹폰트 굵기:
  - Sans KR: 400, 500, 600, 700
  - Mono: 400, 500
- 본문에는 운영체제별 글꼴 스무딩을 적용한다.

### 5.2 타입 스케일

| 역할      | 크기                             | 행간 | 자간     | 굵기     | 사용                         |
| --------- | -------------------------------- | ---- | -------- | -------- | ---------------------------- |
| `hero`    | 34px 기준, `min(2.125rem, 12vw)` | 1.05 | -0.022em | 700      | 월 수지, 거래 시트 금액      |
| `title`   | 20px                             | 1.25 | -0.012em | 700      | 로그인 제품명, 시트 제목     |
| `section` | 15px                             | 1.35 | -0.006em | 600      | 섹션 제목, 목록 주요명, 금액 |
| `body`    | 14px                             | 1.55 | 기본     | 기본 400 | 본문, 폼 값, 버튼            |
| `caption` | 12px                             | 1.45 | 0.008em  | 500      | 라벨, 도움말, 메타 정보      |
| `micro`   | 11px                             | 1.3  | 0.02em   | 600      | 하단 탭, 아이콘 그리드 라벨  |

`micro`는 아이콘 바로 아래의 짧은 라벨에만 사용하는 예외 크기다. 차트 축도 공간 제약 때문에
11px를 사용하지만, 같은 값은 툴팁이나 설명으로 다시 제공한다.

### 5.3 숫자 규칙

- 모든 금액과 주요 백분율은 `.tnum`으로 tabular figures를 사용한다.
- 금액 자간은 `-0.2px`로 조정한다.
- 수입은 목록에서 `+`와 자금 수입색을 사용한다.
- 지출·저축·투자는 목록에서 음수 부호로 표시하지만 기본 `ink`를 유지한다.
- 월 수지는 양수면 `+`와 성공색, 음수면 `-`와 오류색을 사용한다.
- 축약 금액은 차트 축과 중앙 라벨에서만 사용하고, 툴팁은 전체 원화 값을 보여준다.

### 5.4 문장 조판

- 긴 설명은 `text-pretty`로 고른 줄바꿈을 유도한다.
- 빈 상태 설명은 최대 **30ch**로 제한한다.
- 카테고리명, 메모, 이메일 등 한 줄 정보는 필요한 곳에서 말줄임 처리한다.
- 본문은 양쪽 맞춤을 사용하지 않는다.

## 6. 형태, 경계, 그림자

### 6.1 모서리

| 토큰      | 값          | 사용                                    |
| --------- | ----------- | --------------------------------------- |
| `control` | 8px         | 입력, 작은 버튼, 행 호버, 아이콘 선택   |
| `surface` | 12px        | 카드, 배너, 필터 그룹, 일반 패널        |
| `hero`    | 18px        | 월 수지 히어로, 큰 금액 입력, 바텀 시트 |
| `full`    | 9999px 성격 | 칩, 필, 토글, 탭 아이콘 캡슐, 원형 버튼 |

### 6.2 표면 계층

`Card`는 세 수준을 가진다.

| 수준      | 표현                                 | 사용                            |
| --------- | ------------------------------------ | ------------------------------- |
| `flat`    | 별도 배경·테두리 없음, 12px 모서리만 | 이미 섹션 표면 안에 있는 콘텐츠 |
| `surface` | `paper` + `line` 테두리 + 12px       | 일반 카드, 차트, 설정 그룹      |
| `hero`    | `paper` + `line` 테두리 + 18px       | 화면의 가장 중요한 한 항목      |

같은 섹션 안의 반복 항목은 카드 여러 개보다 `line-soft` 구분선을 우선한다.

### 6.3 그림자

| 토큰     | 값의 성격                 | 사용                                                |
| -------- | ------------------------- | --------------------------------------------------- |
| `nav`    | 위쪽으로 얕고 넓은 그림자 | 고정 하단 탭                                        |
| `raised` | 2px 아래, 6px 번짐        | 선택된 segmented 항목, 토글 손잡이, 추가 버튼, 툴팁 |
| `sheet`  | 위쪽으로 8px, 32px 번짐   | 바텀 시트                                           |

일반 카드는 그림자를 사용하지 않는다. 떠 있는 탐색·시트·일시적 메뉴만 그림자를 가진다.

## 7. 간격과 크기

Tailwind의 rem 기반 간격을 사용하며, 화면에서 반복되는 핵심 리듬은 다음과 같다.

| 값   | 대표 사용                                  |
| ---- | ------------------------------------------ |
| 4px  | 아이콘 내부 간격, 작은 목록 간격           |
| 6px  | 라벨–입력, 조밀한 텍스트 행                |
| 8px  | 행·버튼 내부 간격, 섹션 제목–내용          |
| 10px | 필터 그룹 내부 세로 간격, 행 패딩 일부     |
| 12px | 카드 기본 패딩, 폼 요소 사이 보조 간격     |
| 14px | AI 카드 세로 패딩                          |
| 16px | 화면 좌우 패딩, 카드 큰 패딩, 폼 기본 간격 |
| 20px | 히어로 카드 세로 패딩, 시트 하단 기본 여백 |
| 24px | 요약 화면의 큰 블록 간격                   |
| 28px | 설정 화면의 최상위 섹션 간격               |
| 32px | 로그인 헤더와 폼 사이                      |

주요 높이:

- 텍스트 입력·칩: 최소 **44px**
- segmented 항목: 최소 **40px**
- 기본 버튼: 최소 **48px**
- 거래·설정 행: 최소 **48–56px**
- 앱 바: **56px**
- 하단 탭: 최소 **56px**
- 작은 아이콘 버튼의 유효 터치 영역: **44×44px**
- 카테고리 순서 화살표: 각각 **44×28px**. 세로로 붙어 있어 터치 영역 중첩을 피한 예외다.

## 8. 아이콘

### 8.1 스타일

- 대부분 20×20 viewBox 기반의 인라인 SVG다.
- 기본 선 굵기: **1.5–1.7px**
- 선 끝과 모서리: round
- 색: `currentColor`
- 장식 아이콘은 `aria-hidden` 처리한다.
- 아이콘만 있는 버튼에는 반드시 한국어 접근 가능한 이름을 제공한다.

### 8.2 카테고리 아이콘 세트

현재 선택 가능한 아이콘은 24개다.

| 키        | 표시 라벨 | 대표 의미          |
| --------- | --------- | ------------------ |
| `food`    | 식비      | 음식·외식          |
| `cafe`    | 카페      | 커피·카페          |
| `home`    | 주거      | 집·월세·관리비     |
| `bus`     | 교통      | 대중교통·택시      |
| `cart`    | 쇼핑      | 장보기·생활·쇼핑   |
| `health`  | 건강      | 병원·약            |
| `fun`     | 여가      | 문화·취미          |
| `pay`     | 요금      | 공과금·통신·구독   |
| `bank`    | 은행      | 금융기관           |
| `save`    | 저축      | 적금·비상금        |
| `invest`  | 투자      | 주식·펀드          |
| `income`  | 수입      | 월급·보너스        |
| `gift`    | 선물      | 선물·경조          |
| `edu`     | 교육      | 학원·책            |
| `clothes` | 의류      | 옷·패션            |
| `travel`  | 여행      | 숙박·항공          |
| `fitness` | 운동      | 헬스·스포츠        |
| `phone`   | 휴대폰    | 통신·인터넷        |
| `pet`     | 반려동물  | 강아지·고양이      |
| `beauty`  | 미용      | 뷰티·헤어          |
| `car`     | 자동차    | 차량·주유          |
| `music`   | 음악      | 음악 스트리밍      |
| `game`    | 게임      | 게임·영상 구독     |
| `etc`     | 기타      | 분류되지 않은 항목 |

새 카테고리는 이름의 한국어 키워드로 기본 아이콘을 추정한다. 일치하지 않으면 수입은
`income`, 지출은 `cart`, 저축은 `save`, 투자는 `invest`를 사용한다.

### 8.3 탐색 아이콘

- 요약: 세로 막대 3개
- 내역: 가로 목록선 3개
- 통계: 상승·하락折線
- 설정: 슬라이더 2개
- 추가: 원 안의 `＋`
- 월 이동: 좌우 꺾쇠
- 엑셀: 문서와 아래 화살표
- 펼치기: 회전하는 아래 꺾쇠
- 닫기: `×`
- 오류: 원 안의 느낌표
- 권한: 자물쇠

### 8.4 브랜드 표시와 파비콘

- 앱 화면 안에는 별도 심볼 로고가 없고 `가계부` 텍스트가 제품명 역할을 한다.
- 브라우저 파비콘은 64×64px, 14px 모서리의 밝은 정사각형 안에 짙은 장부를 그린
  일러스트다.
- 장부 왼쪽의 녹색 책등, 흰색 기록선, 하단의 황토·녹색·흰색 막대 차트로 “기록과
  재무 요약”을 함께 표현한다.
- 파비콘의 `#fbfaf8`, `#39352f`, `#7d9377`, `#b09863`, `#e9e5de`는 현재 UI 토큰과
  유사하지만 정확히 일치하지 않는 이전 팔레트다.
- `public/icons.svg`의 소셜·문서 아이콘 스프라이트는 현재 앱에서 참조하지 않으므로 적용
  중인 디자인 자산에는 포함하지 않는다.

## 9. 핵심 컴포넌트

### 9.1 섹션 헤더

- 제목: 실제 `<h2>`, `section` 스타일
- 보조 문구: `caption`, `ink2`
- 액션: 우측 `TextAction`
- 제목과 보조 정보는 baseline으로 정렬한다.

### 9.2 Pill과 StatusPill

- 높이가 작은 읽기 전용 라벨
- 완전한 캡슐 모양
- `micro` 글자, 좌우 8px, 상하 4px
- 상태 필은 상태별 12% 틴트 배경과 같은 계열 글자색을 사용한다.
- 상태 이름 자체가 의미의 주 전달자다.

### 9.3 Chip

- 필터나 카테고리 단일 선택에 사용한다.
- 최소 높이: 44px
- 기본:
  - `paper` 배경
  - `line` 테두리
  - `ink2` 글자
- 호버: 테두리와 글자가 진해진다.
- 선택:
  - `ink` 배경
  - `paper` 글자
  - `aria-pressed=true`
- 여러 칩은 가로 스크롤하지 않고 줄바꿈한다.

### 9.4 Segmented control

- 로그인/회원가입, 자금 구분 선택에 사용한다.
- 전체 컨테이너: `fill1`, 12px 모서리, 4px 내부 여백
- 각 항목: 최소 40px, 동일 너비
- 선택 항목: `paper`, `ink`, `raised` 그림자
- 비선택 항목: `ink2`, 호버 시 `ink`
- `role="group"`과 각 버튼의 `aria-pressed`를 사용한다.

### 9.5 Toggle

- 시각 크기: **38×22px**
- 손잡이: **18×18px**
- 켜짐: `ink` 트랙, 오른쪽 손잡이
- 꺼짐: `fill3` 트랙, 왼쪽 손잡이
- 비활성: 40% 불투명도, 금지 커서
- 실제 터치 영역은 44×44px로 확장한다.
- `role="switch"`와 `aria-checked`를 사용한다.

### 9.6 Progress

- 완전한 캡슐형 트랙
- 트랙: `fill2`
- 기본 높이: 6px
- 자금 흐름 셀은 4px
- 값은 0–100으로 제한한다.
- 같은 값이 인접 텍스트에 있어 진행 바 자체는 스크린 리더에서 숨긴다.

### 9.7 버튼 계층

| 종류        | 스타일                                    | 사용                         |
| ----------- | ----------------------------------------- | ---------------------------- |
| Primary     | `ink` 배경, `paper` 글자                  | 저장, 로그인, 적용           |
| Ghost       | `fill1` 배경 + `line` 테두리, `ink2` 글자 | 더 보기, 취소                |
| Danger      | `status-danger` 배경, `paper` 글자        | 최종 삭제 확인               |
| Text action | 배경 없는 `caption` 굵은 글자             | 추가, 다시 생성, 펼치기 보조 |
| Icon button | 원형, 아이콘만 표시                       | 월 이동, 닫기, 순서 변경     |

기본 버튼은 폭 100%, 최소 높이 48px, 12px 모서리다. 위험색은 되돌리기 어려운 최종
확인에만 사용하고, 삭제 진입은 먼저 텍스트 액션으로 제공한다.

### 9.8 공통 상호작용 상태

`pressable`을 사용하는 컨트롤은 다음 상태를 공유한다.

- 상태 전환: **140ms**
- 이징: `cubic-bezier(0.23, 1, 0.32, 1)`
- 누름: `scale(0.97)`
- 비활성: 투명도 감소와 금지 커서
- 키보드 포커스: `ink` 2px 아웃라인, 2px 오프셋
- 호버: 배경·테두리·글자 중 해당 컴포넌트에 필요한 속성만 진해진다.

### 9.9 폼 필드

공통 입력:

- 최소 높이: 44px
- 8px 모서리
- `paper` 배경
- `line` 테두리
- 좌우 12px, 상하 10px
- 기본 글자: `body`, `ink`
- 플레이스홀더: `ink3`
- 호버: `ink3` 테두리
- 포커스: `ink` 테두리와 전역 포커스 링
- 비활성: 50% 불투명도

필드 구조:

1. `caption` 굵은 라벨
2. 입력
3. 선택적 도움말 또는 오류

오류는 `status-danger` 글자와 원형 느낌표를 함께 사용하고, 입력과 `aria-describedby`로
연결한다.

특수 입력:

- **비밀번호**: 우측 44px 보기/숨기기 버튼, 전체 복합 필드에 포커스 링
- **금액 field**: `₩` + 우측 정렬 금액, 일반 입력 크기
- **금액 hero**: 18px 모서리, 큰 `hero` 숫자, 거래 시트의 최우선 필드
- **Select**: 네이티브 선택 의미는 유지하지만 `appearance: none`으로 기본 화살표를
  표시하지 않는다.
- **날짜·월**: tabular figures

### 9.10 피드백

| 상태      | 표현                                             |
| --------- | ------------------------------------------------ |
| 로딩      | 화면 구조를 닮은 정적 스켈레톤                   |
| 짧은 대기 | 20px 회전 스피너 + 설명                          |
| 빈 데이터 | 중앙 정렬 제목, 선택적 30ch 설명, 선택적 액션    |
| 일반 오류 | 오류 아이콘 + danger 틴트 배너                   |
| 권한 오류 | 자물쇠 + warning 틴트 + “권한이 없습니다” 접두어 |
| 성공      | success 테두리·틴트·글자와 `role=status`         |
| 필드 오류 | 입력 아래 작은 아이콘과 오류 문장                |

화면 로딩 스켈레톤은 애니메이션하지 않는다. 실제 카드·행·차트의 크기를 먼저 예약해
콘텐츠가 나타날 때 레이아웃이 크게 이동하지 않게 한다.

## 10. 화면별 디자인

### 10.1 로그인·회원가입

- `canvas` 전체 배경 위 중앙 정렬
- 폼 최대 너비: **360px**
- 좌우 패딩: 24px
- 상단:
  - `가계부` title
  - 직접 기록 제품 설명 body
  - 계정별 보관과 비연동 안내 caption
- 로그인/회원가입은 segmented control로 전환한다.
- 이메일과 비밀번호는 세로 단일 컬럼이다.
- 결과 메시지는 제출 버튼 바로 위의 고정된 정보 위치에 나온다.
- 제출 버튼은 primary full-width다.

### 10.2 요약

상단에서 아래로 읽는 순서는 다음과 같다.

1. **월 탐색**
2. **이번 달 남는 돈**
3. **자금 흐름**
4. **달성 확인**
5. **과거 월 마감 점검**
6. **AI 인사이트**
7. **상세 분석**

#### 수지 히어로

- 화면의 유일한 hero 카드와 hero 숫자
- 제목: “이번 달 남는 돈 (수지)”
- 금액 아래 계산식: `수입 − 지출 − 저축 − 투자`
- 양수는 success, 음수는 danger

#### 자금 흐름

- 하나의 surface 카드를 2×2로 분할
- 각 셀: 자금색 점, 유형명, 금액, 수입 대비 비중 막대
- 카드 네 개를 반복하지 않고 내부 구분선을 사용한다.

#### 달성 확인

- 지출 예산과 저축 목표를 한 목록에 표시한다.
- 각 행: 카테고리·구분, 상태 필, 상태 진행 바, 실제/목표, 퍼센트
- 현재 월의 opt-in 지출 예산에는 남은 일수·하루 허용액 힌트를 추가한다.
- 목표가 없으면 “예산 없음” 또는 “목표 없음”과 `—`를 표시한다.
- 현재 월은 아직 조정 가능한 항목을 먼저 배치한다.

#### 월 마감 점검

- 과거 월이며 AI 사용이 켜졌을 때만 보인다.
- 기본은 접힌 surface 패널이다.
- 전체 폭 행을 눌러 열며, 텍스트와 회전 꺾쇠가 상태를 함께 표시한다.
- AI 요약은 `fill1`, 원장 점검 결과는 기본 paper 표면에 둔다.
- 결과 그룹은 “확인 필요”와 “참고”로 명시한다.

#### AI 인사이트

- `fill1` 배경, 테두리 없음
- 제목 옆에 “AI가 작성한 조언”을 표시한다.
- 우측에 `다시 생성` 텍스트 액션
- 결과는 작은 점이 있는 bullet 목록
- AI 사용이 꺼졌거나 운영 플래그가 꺼지면 블록 전체를 숨긴다.

#### 상세 분석

1. 지출 카테고리 도넛
2. 구분별 월 합계 막대
3. 월별 수지 영역 차트

모든 차트는 surface 카드 안에 배치한다.

### 10.3 내역

#### 상단

- 왼쪽: 월 탐색
- 중앙: 내역 제목
- 오른쪽: 문서 아이콘 + `엑셀`
- 내보내기는 현재 월과 현재 필터 결과를 `.xlsx`로 저장한다.

#### 검색과 필터

- 메모 검색 입력은 독립된 첫 줄
- 아래 surface 필터 그룹:
  - 전체·수입·지출·저축·투자 칩
  - 카테고리 select
- 유형을 바꾸면 선택 카테고리를 초기화한다.
- 칩은 320px와 확대 환경에서 숨지 않도록 줄바꿈한다.

#### 거래 목록

- 날짜별 `<section>`으로 그룹화한다.
- 날짜 헤더는 본문 스크롤 상단에 sticky:
  - 왼쪽 날짜
  - 오른쪽 그날의 순금액
- 거래 행:
  - 40×40px 아이콘 타일
  - 카테고리명
  - 선택적 `고정` 마커
  - 선택적 한 줄 메모
  - 우측 금액
- 행 전체가 수정 시트를 여는 버튼이며 최소 높이는 56px다.
- 수입만 자금 수입색과 `+`를 사용한다.
- 페이지가 더 있으면 하단에 ghost `더 보기` 버튼을 표시한다.

빈 상태는 두 종류를 구분한다.

- 필터 없음: “거래가 없습니다” + 추가 버튼 안내
- 필터 있음: “조건에 맞는 거래가 없습니다” + 조건 변경 안내

### 10.4 거래 추가·수정 시트

표시 순서:

1. 시트 제목과 닫기
2. 추가 모드에서 자연어 입력
3. 고정 거래 수정 안내
4. 자금 구분 segmented
5. hero 금액
6. 카테고리 칩
7. 날짜 + 메모 2열
8. 추가 모드의 `저장 후 계속 입력` 토글
9. 제출 오류
10. 저장 버튼
11. 수정 모드의 삭제 흐름

자연어 입력은 AI 표면인 `fill1`에 놓고, “적용”은 저장이 아니라 폼 초안 채우기임을 안내한다.
AI 적용 성공은 info, 확인이 필요한 필드는 warning, 실패는 danger로 구분한다.

삭제는 2단계다.

1. 중앙의 danger 글자 `삭제` 액션
2. danger 틴트 확인 패널 안의 `취소`와 최종 danger `삭제`

### 10.5 통계

- 상단 제목: 통계
- 본문 첫 줄: 3개월, 6개월, 12개월 선택 칩
- 아래 차트 순서:
  1. 월별 수입·지출·저축·투자 영역 + 점선 수지
  2. 월별 수지 영역 차트
  3. 월별 지출 구성 누적 막대
  4. 카테고리별 지출 비교 가로 막대
- 차트 카드 간 간격: 12px
- 카테고리별 비교 차트는 항목 수에 따라 높이를 늘린다.
- 데이터가 없으면 차트 프레임 안에서 빈 상태를 표시한다.

### 10.6 설정

큰 섹션 순서는 다음과 같다.

1. 가계부 일반
2. 카테고리
3. 고정 항목
4. 인앱 AI
5. 계정

읽기 빈도가 높고 자주 수정하는 원장 항목을 위에, 민감하거나 드물게 쓰는 AI opt-in과
로그아웃을 아래에 둔다.

#### 가계부 일반

- 카드 내부 행: 가계부 이름, `KRW ₩`
- 카드 아래 caption: 달력 월 집계 기준

#### 카테고리

- 헤더 우측 `＋ 추가`는 관리 권한이 있을 때만 표시한다.
- 행 구성:
  - 권한이 있으면 위·아래 순서 버튼
  - 36×36px 아이콘 타일
  - 이름 + 자금 구분 필
  - 예산 또는 목표
  - 활성 토글
- 보조 금액 문구는 지출에 예산, 저축에 목표를 표시한다. 수입·투자는 현재 모두
  `목표 없음`으로 표시된다.
- 비활성 행은 전체 60% 불투명도다.
- 행 편집 영역과 토글 영역을 분리한다.

카테고리 시트:

- 추가 시 자금 구분을 선택하고, 수정 시 구분은 읽기 전용이다.
- 이름, 24개 아이콘 그리드, 예산 또는 목표를 설정한다.
- 아이콘 버튼은 56px 너비, 최소 44px 높이다.
- 지출 예산이 있을 때만 예산 페이스 토글을 제공한다.

#### 고정 항목

- 헤더 우측 `＋ 추가`는 편집 권한이 있을 때만 표시한다.
- 행 구성:
  - 이름 + 자금 구분 필
  - 매월 발생일과 시작·종료월
  - 금액
  - 활성 토글
- 비활성 행은 60% 불투명도다.

고정 항목 시트는 구분, 카테고리, 항목명, 금액, 시작월·종료월, 발생일, 메모 순서다.

#### 인앱 AI

- 처리 고지와 사용 토글을 하나의 카드 안에서 구분선으로 나눈다.
- 처리 고지는 caption 라벨 + body 본문이다.
- 토글 설명은 기능 숨김과 서버 거부를 함께 밝힌다.
- 카드 아래에는 개인정보 처리방침 반영 안내가 있다.

#### 계정

- 이메일은 읽기 전용 행
- 로그아웃은 위험 버튼이 아니라 카드 안의 중립적 텍스트 행

## 11. 차트 문법

### 11.1 공통 프레임

- `paper` + `line` surface 카드
- 상단 제목은 `section`
- 범례는 `caption`
- 좁은 화면에서는 제목과 범례가 압축되지 않고 다음 줄로 감긴다.
- 툴팁은 12px 모서리, 테두리, `raised` 그림자다.
- 툴팁 행은 색 점, 시리즈명, 전체 원화 값, 선택적 백분율로 구성한다.

### 11.2 축과 그리드

- 축 글자: 11px
- X축: `ink3`
- 카테고리 축: `ink2`
- Y축 너비: 40px
- 축선과 tick line은 숨긴다.
- 그리드: `line-soft`, `3 3` 점선
- 0 기준선: `line`, 1.5px

### 11.3 차트 종류

- 수지: monotone 영역 차트, 2.4px `ink` 선
- 수지 점:
  - 0 이상: 저축색
  - 0 미만: 지출색
- 수지 면:
  - 양수: 저축색 30% → 2% 그라데이션
  - 음수: 지출색 30% → 2% 그라데이션
- 월별 네 자금: 각 자금색 2px 영역선, 8% 채움
- 수지 비교선: `ink`, 2.2px, `4 3` 점선
- 도넛: 안쪽 54px, 바깥 80px, 조각 간 2도
- 세로 막대: 위쪽 5px 반경, 최대 48px
- 누적 막대: 최상단 시리즈에만 위쪽 4px 반경, 최대 40px
- 가로 막대: 오른쪽 5px 반경, 최대 22px

### 11.4 차트 접근성

- 차트 전체를 하나의 `role="img"`로 제공한다.
- 제목과 이미 계산된 핵심 값으로 문장형 `aria-label`을 만든다.
- Recharts 내부 키보드 레이어는 비활성화해 의미 없는 SVG 포커스 정지를 제거한다.
- 도넛 조각의 root tab stop도 제거한다.
- 색과 함께 범례, 선 모양, 텍스트 설명을 제공한다.

## 12. 모션

### 12.1 시간과 이징

| 용도           | 시간  | 이징                             |
| -------------- | ----- | -------------------------------- |
| 버튼·상태 변화 | 140ms | `cubic-bezier(0.23, 1, 0.32, 1)` |
| 패널·시트      | 200ms | `cubic-bezier(0.32, 0.72, 0, 1)` |

### 12.2 적용

- pressable 누름: 0.97 축소
- 토글: 손잡이의 left 위치 전환
- 월 마감: 꺾쇠 180도 회전
- 바텀 시트: 아래에서 위로 이동
- 바텀 시트 backdrop: 투명 → `ink` 32% 혼합
- 스피너: 회전
- 스켈레톤: 정적

### 12.3 모션 감소

`prefers-reduced-motion: reduce`에서는:

- 모든 애니메이션·전환 시간을 사실상 0으로 줄인다.
- 반복 애니메이션은 1회로 제한한다.
- pressable 축소를 제거한다.
- 시트 이동을 제거한다.
- 부드러운 스크롤을 사용하지 않는다.

## 13. 반응형과 확대

- 기본 지원 폭은 **320–480px**다.
- 480px보다 넓어져도 태블릿·데스크톱용 다단 레이아웃으로 바뀌지 않는다.
- 하단 탭과 바텀 시트도 앱의 480px 열에 맞춘다.
- `viewport-fit=cover`와 세 가지 safe-area 유틸리티를 사용한다.
- `maximum-scale`이나 `user-scalable=no`를 쓰지 않아 확대를 허용한다.
- 글자 크기는 rem 기반이다.
- hero 금액은 200% 텍스트 확대에서도 화면을 넘지 않도록 `12vw` 상한을 둔다.
- 탭은 `min-width: 0`과 최대 너비 제한으로 확대 시 마지막 탭이 밀려나지 않게 한다.
- 필터·카테고리 칩과 차트 헤더·범례는 줄바꿈한다.
- 날짜/메모처럼 두 칸으로 유지되는 폼은 각 열에 `min-width: 0`을 적용한다.
- 긴 목록 정보는 말줄임하고, 긴 설명은 자연스럽게 여러 줄로 흐르게 한다.
- 가로 스크롤을 주요 적응 전략으로 사용하지 않는다.

## 14. 접근성

현재 구현된 기준:

- 문서 언어: `ko`
- 경로별 문서 제목: `화면명 · 가계부`
- 구조 요소: `header`, `main`, `nav`, `section`, `search`, 실제 heading
- 모든 키보드 조작 요소에 통일된 `:focus-visible`
- 아이콘 전용 버튼에 `aria-label`
- 토글에 `role="switch"`와 `aria-checked`
- 선택 칩·segmented·아이콘 선택에 `aria-pressed`
- 펼침 컨트롤에 `aria-expanded`
- 그룹형 선택에 이름 있는 `role="group"`
- 필드 오류를 `aria-invalid`와 `aria-describedby`로 연결
- 오류는 `role="alert"`, 일반 동적 결과는 `role="status"`
- 로딩 스켈레톤은 장식 블록을 숨기고 화면 단위 상태 한 번만 알림
- 바텀 시트는 네이티브 dialog의 focus trap, inert 배경, Escape, 초점 복원을 사용
- 상태 의미를 색만으로 전달하지 않음
- 차트에 문장형 대체 설명 제공
- 장식 SVG와 진행 바는 접근성 트리에서 제외
- 비밀번호 표시 상태와 경고를 보조 기술에 제공
- 최소 44px 중심의 터치 영역
- 200% 확대와 모션 감소 고려

현재 명시되지 않은 것:

- 반복 헤더를 건너뛰는 skip link는 없다.
- 차트의 별도 데이터 표 보기 기능은 없다.
- 다크 모드는 없다.
- 일반 폼 입력 글자는 현재 `body`인 14px이므로, iOS에서 입력 포커스 시 자동 확대가
  발생할 가능성이 있다.
- 실제 스크린 리더·키보드·고대비 모드 사용성은 코드만으로 보장되지 않으며 별도 수동
  검증이 필요하다.

## 15. 권한과 조건부 UI

- 인증 전에는 로그인 화면만 보인다.
- 거래 추가 권한이 없으면 추가 시트 안에서 읽기 전용 안내를 보여준다.
- 카테고리 `＋ 추가`, 순서 변경, 편집, 활성 토글은 관리 권한이 있을 때만 보인다.
- 고정 항목 `＋ 추가`, 편집, 활성 토글은 편집 권한이 있을 때만 보인다.
- 권한이 없는 사용자는 사용할 수 없는 액션을 비활성 버튼으로 남기기보다 숨기는 경우가 많다.
- AI 관련 UI는 사용자 설정과 운영 플래그에 따라 블록 전체가 사라질 수 있다.
- 월 마감 점검은 현재 월이 아닌 과거 월에서만 보인다.
- 예산 페이스는 현재 월의 opt-in 지출 카테고리에서만 보인다.

## 16. 콘텐츠 원칙

- 사용자 행동을 구체적으로 말한다: `엑셀`, `다시 생성`, `저장 후 계속 입력`.
- 시스템 용어보다 결과를 말한다: “조건에 맞는 거래가 없습니다”.
- AI 생성 콘텐츠는 반드시 “AI가 작성한 조언/요약”, “AI가 제안한 초안”으로 출처를
  드러낸다.
- 자연어 초안은 자동 저장되지 않으며 확인 후 저장해야 함을 반복해서 알린다.
- 위험한 행동은 질문형 확인 문장으로 한 번 더 묻는다.
- 권한 오류, 일반 오류, 정보, 경고를 같은 어조나 같은 색으로 합치지 않는다.
- 금액·월·기간은 구체적으로 쓰고, 범용적인 “보내기” 같은 모호한 동사를 피한다.

## 17. 현재 디자인의 명시적 경계

다음은 현재 구현에 없는 요소이며, 디자이너가 후속 안을 만들 때 기존 기능으로 오해하지
않아야 한다.

- 다크 모드와 테마 선택
- 태블릿·데스크톱 전용 레이아웃
- 사이드바나 상단 글로벌 내비게이션
- 차트 시리즈 on/off, 이전 기간 오버레이, 차트 데이터 표
- 거래 필터 전체 초기화 버튼
- 바텀 시트 드래그 제스처
- 페이지 단위 모션이나 화면 전환 애니메이션
- 토스트 시스템
- 툴팁형 도움말
- 원장 전환 UI
- 카테고리 검색
- 별도 온보딩 플로우
- 은행·카드 연결

## 18. 디자이너–개발자 핸드오프 규칙

새 디자인을 제안하거나 구현할 때 현재 시스템과 맞추려면 다음을 따른다.

1. 색, 글자 크기, 모서리, 그림자를 임의 값으로 추가하기 전에 `src/index.css` 토큰으로
   표현 가능한지 확인한다.
2. 자금 유형에는 `Fund` 색, UI 상태에는 `Status` 색만 사용한다.
3. 화면당 hero 표면과 hero 숫자는 하나를 원칙으로 한다.
4. 같은 섹션의 반복 행은 새 카드를 늘리기보다 구분선과 여백을 먼저 사용한다.
5. AI 생성문은 계산값과 같은 paper 카드에 섞지 않고 `fill1` 표면과 출처 라벨을 사용한다.
6. 아이콘만 있는 액션은 44px 유효 영역과 텍스트 대체 이름을 함께 정의한다.
7. 선택 상태는 색 외에 채움, 굵기, 텍스트 또는 ARIA 상태를 추가한다.
8. 320px, 480px, 200% 텍스트 확대, iOS safe area에서 우선 검토한다.
9. 필터와 범례는 숨기는 가로 스크롤보다 줄바꿈을 우선한다.
10. 로딩·빈 데이터·필터 결과 없음·권한 오류·일반 오류를 각각 설계한다.
11. 삭제와 같은 파괴적 액션만 danger primary를 사용한다.
12. 차트를 추가하면 제목, 단위, 범례, 툴팁, 빈 상태, 문장형 대체 설명을 한 세트로
    정의한다.

## 19. 구현 위치

| 디자인 영역                             | 소스                                                |
| --------------------------------------- | --------------------------------------------------- |
| 토큰, 전역 포커스, 모션, safe area      | `src/index.css`                                     |
| 카드, 버튼, 칩, segmented, 토글, 진행률 | `src/ui/primitives.tsx`                             |
| 필드, 입력, 비밀번호, 금액              | `src/ui/forms.tsx`                                  |
| 앱 셸, 앱 바, 월 탐색, 하단 탭          | `src/ui/layout.tsx`                                 |
| 로딩, 빈 상태, 오류                     | `src/ui/feedback.tsx`, `src/ui/skeletons.tsx`       |
| 바텀 시트                               | `src/ui/BottomSheet.tsx`                            |
| 톤과 색 매핑                            | `src/ui/tone.ts`, `src/ui/statusTone.ts`            |
| 카테고리 아이콘                         | `src/ui/Glyph.tsx`, `src/ui/glyphForCategory.ts`    |
| 차트 공통 문법                          | `src/ui/charts.tsx`, `src/ui/BalanceTrendChart.tsx` |
| 로그인                                  | `src/features/auth/LoginPage.tsx`                   |
| 요약                                    | `src/features/dashboard/*`                          |
| 내역과 거래 시트                        | `src/features/transactions/*`                       |
| 통계                                    | `src/features/reports/*`                            |
| 설정과 관리 시트                        | `src/features/settings/*`                           |
| 폰트, viewport, 라이트 모드 선언        | `index.html`                                        |
